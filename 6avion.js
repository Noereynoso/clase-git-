
let espacios = "     ";
console.log (espacios);

let VelocidadMaxima = 1200;
let vel = "velocidad maxima =";
    console.log (vel, +" "+ VelocidadMaxima);
let VelocidadMinima = 600
let Vel = "velocidad minima =";
    console.log (Vel, +" "+ VelocidadMinima);
let AlturaMaxima = 2000
let Alt = "AlturaMaxima =";
    console.log (Alt, +" "+ AlturaMaxima);
let AlturaMinima = 50
let alt = "AlturaMinima =";
    console.log (alt, +" "+ AlturaMinima);
let VelocidadAterrizaje = 250
let velatrr = "VelocidadAterrizaje =";
    console.log (velatrr, +" "+ VelocidadAterrizaje);
let AlturaAterrizaje = 1
let Altatrr = "VelocidadAterrizaje =";
    console.log (Altatrr, +" "+ AlturaAterrizaje);

    let espacio = "     ";
        console.log (espacio);

    // velocidades correspondientes //

let Velocidad = VelocidadMinima < VelocidadMaxima;
    switch (VelocidadMinima){
        case ( VelocidadMinima ) :
            if (VelocidadMinima < VelocidadMaxima ){
                console.log ("VelocidadMinima < VelocidadMaxima"); 
            console.log("el avion puede aterrizar")
            console.log (Velocidad);

            let espacio = "     ";
            console.log (espacio);

    // velocidades No correspondientes //

let Velocidades = "VelocidadMinima > VelocidadMaxima";
    console.log (Velocidades);
    switch (VelocidadMinima){
        case ( VelocidadMinima ) : 
            if (VelocidadMinima > VelocidadMaxima)
                console.log ("VelocidadMinima > VelocidadMaxima");
            console.log ("el avion NO puede aterrizar")

let velocidadesT = true
        console.log (velocidadesT);

        let espacio = "     ";
        console.log (espacio);

    // velocidades En Las Alturas //

let alturas = AlturaMaxima > AlturaMinima;
    switch (AlturaMaxima){
        case (AlturaMaxima) :
            if ( AlturaMaxima > AlturaMinima)
                console.log ("AlturaMaxima > AlturaMinima")
                console.log ("el avion SI puede aterrizar")
            console.log (alturas);

    let espacio = "     ";
        console.log (espacio);
        
    // velocidades En Las Alturas //

let altura = "AlturaMaxima < AlturaMinima";
    console.log (altura);
    switch (AlturaMaxima){
        case (AlturaMaxima) :
            if ( AlturaMaxima < AlturaMinima)
            console.log ("AlturaMaxima < AlturaMinima")
            console.log ("el avion NO puede aterriszar ")
let alturasM = true 
        console.log (alturasM);
    }           
}        
}
}
}