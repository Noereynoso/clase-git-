// traductor //
let todasLasPalabras = "palabras para traducir";
    console.log (todasLasPalabras);
let palabras = "Gato, Perro, Puerta, Ventana, Mesa,";
let usoTraductor = "cat, dog, door, windows, table,"
let palabrastraducidas = "palabras traducidas";
    console.log (palabras); 
    console.log (palabrastraducidas);
    switch (palabras) {
      case (palabras) :
        if ("traducido", " ") {
          console.log (" ", usoTraductor );
        }

    }
let preguntas = "¿como se dice gato y perro en inlges?"
let respuestaDelTraductor = "cat y dog";
    console.log (preguntas);
    switch (preguntas) {
      case (preguntas) :
        if ("Traducido", " ") {
          console.log (" ", respuestaDelTraductor);
        }
    } 
 
    let pregunta = "¿como se dice puerta ventana y mesa en inlges?"
    let respuestaDeTraductor = "door, windows, table, ";
        console.log (pregunta);
        switch (pregunta) {
          case (pregunta) :
            if ("Traducido", " ") {
              console.log (" ", respuestaDeTraductor);
            }
        }







